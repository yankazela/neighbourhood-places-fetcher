let H;_441‍.x([["getPlace",()=>getPlace]]);_441‍.w("here-js-api/scripts/mapsjs-places",[["default",["H"],function(v){H=v}]]);

let logger = global.logger
let siteProps = global.siteProps

let platform = new H.service.Platform({
  app_id: siteProps.google_app_id,
  app_code: siteProps.google_app_code
})

let search = new H.places.Search(platform.getPlacesService())

       async function getPlace (placeType, longitude, latitude) {
  let at = longitude + ',' + latitude
  let params = {
    q: placeType,
    at: at
  }
  return search.request(params, {}, onResult, onError)
}

function onResult (data) {
  return data
}
function onError (data) {
  logger.error(data.message)
}
